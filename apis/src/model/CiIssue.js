/**
 * App Store Connect API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 4.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CiIssueAttributes from './CiIssueAttributes';
import ResourceLinks from './ResourceLinks';

/**
 * The CiIssue model module.
 * @module model/CiIssue
 * @version 4.1
 */
class CiIssue {
    /**
     * Constructs a new <code>CiIssue</code>.
     * @alias module:model/CiIssue
     * @param type {module:model/CiIssue.TypeEnum} 
     * @param id {String} 
     */
    constructor(type, id) { 
        
        CiIssue.initialize(this, type, id);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type, id) { 
        obj['type'] = type;
        obj['id'] = id;
    }

    /**
     * Constructs a <code>CiIssue</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CiIssue} obj Optional instance to populate.
     * @return {module:model/CiIssue} The populated <code>CiIssue</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CiIssue();

            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('attributes')) {
                obj['attributes'] = CiIssueAttributes.constructFromObject(data['attributes']);
            }
            if (data.hasOwnProperty('links')) {
                obj['links'] = ResourceLinks.constructFromObject(data['links']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CiIssue</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CiIssue</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CiIssue.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // validate the optional field `attributes`
        if (data['attributes']) { // data not null
          CiIssueAttributes.validateJSON(data['attributes']);
        }
        // validate the optional field `links`
        if (data['links']) { // data not null
          ResourceLinks.validateJSON(data['links']);
        }

        return true;
    }


}

CiIssue.RequiredProperties = ["type", "id"];

/**
 * @member {module:model/CiIssue.TypeEnum} type
 */
CiIssue.prototype['type'] = undefined;

/**
 * @member {String} id
 */
CiIssue.prototype['id'] = undefined;

/**
 * @member {module:model/CiIssueAttributes} attributes
 */
CiIssue.prototype['attributes'] = undefined;

/**
 * @member {module:model/ResourceLinks} links
 */
CiIssue.prototype['links'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
CiIssue['TypeEnum'] = {

    /**
     * value: "ciIssues"
     * @const
     */
    "ciIssues": "ciIssues"
};



export default CiIssue;

