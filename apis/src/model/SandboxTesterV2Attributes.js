/**
 * App Store Connect API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 4.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TerritoryCode from './TerritoryCode';

/**
 * The SandboxTesterV2Attributes model module.
 * @module model/SandboxTesterV2Attributes
 * @version 4.1
 */
class SandboxTesterV2Attributes {
    /**
     * Constructs a new <code>SandboxTesterV2Attributes</code>.
     * @alias module:model/SandboxTesterV2Attributes
     */
    constructor() { 
        
        SandboxTesterV2Attributes.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SandboxTesterV2Attributes</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SandboxTesterV2Attributes} obj Optional instance to populate.
     * @return {module:model/SandboxTesterV2Attributes} The populated <code>SandboxTesterV2Attributes</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SandboxTesterV2Attributes();

            if (data.hasOwnProperty('firstName')) {
                obj['firstName'] = ApiClient.convertToType(data['firstName'], 'String');
            }
            if (data.hasOwnProperty('lastName')) {
                obj['lastName'] = ApiClient.convertToType(data['lastName'], 'String');
            }
            if (data.hasOwnProperty('acAccountName')) {
                obj['acAccountName'] = ApiClient.convertToType(data['acAccountName'], 'String');
            }
            if (data.hasOwnProperty('territory')) {
                obj['territory'] = TerritoryCode.constructFromObject(data['territory']);
            }
            if (data.hasOwnProperty('applePayCompatible')) {
                obj['applePayCompatible'] = ApiClient.convertToType(data['applePayCompatible'], 'Boolean');
            }
            if (data.hasOwnProperty('interruptPurchases')) {
                obj['interruptPurchases'] = ApiClient.convertToType(data['interruptPurchases'], 'Boolean');
            }
            if (data.hasOwnProperty('subscriptionRenewalRate')) {
                obj['subscriptionRenewalRate'] = ApiClient.convertToType(data['subscriptionRenewalRate'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SandboxTesterV2Attributes</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SandboxTesterV2Attributes</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['firstName'] && !(typeof data['firstName'] === 'string' || data['firstName'] instanceof String)) {
            throw new Error("Expected the field `firstName` to be a primitive type in the JSON string but got " + data['firstName']);
        }
        // ensure the json data is a string
        if (data['lastName'] && !(typeof data['lastName'] === 'string' || data['lastName'] instanceof String)) {
            throw new Error("Expected the field `lastName` to be a primitive type in the JSON string but got " + data['lastName']);
        }
        // ensure the json data is a string
        if (data['acAccountName'] && !(typeof data['acAccountName'] === 'string' || data['acAccountName'] instanceof String)) {
            throw new Error("Expected the field `acAccountName` to be a primitive type in the JSON string but got " + data['acAccountName']);
        }
        // ensure the json data is a string
        if (data['subscriptionRenewalRate'] && !(typeof data['subscriptionRenewalRate'] === 'string' || data['subscriptionRenewalRate'] instanceof String)) {
            throw new Error("Expected the field `subscriptionRenewalRate` to be a primitive type in the JSON string but got " + data['subscriptionRenewalRate']);
        }

        return true;
    }


}



/**
 * @member {String} firstName
 */
SandboxTesterV2Attributes.prototype['firstName'] = undefined;

/**
 * @member {String} lastName
 */
SandboxTesterV2Attributes.prototype['lastName'] = undefined;

/**
 * @member {String} acAccountName
 */
SandboxTesterV2Attributes.prototype['acAccountName'] = undefined;

/**
 * @member {module:model/TerritoryCode} territory
 */
SandboxTesterV2Attributes.prototype['territory'] = undefined;

/**
 * @member {Boolean} applePayCompatible
 */
SandboxTesterV2Attributes.prototype['applePayCompatible'] = undefined;

/**
 * @member {Boolean} interruptPurchases
 */
SandboxTesterV2Attributes.prototype['interruptPurchases'] = undefined;

/**
 * @member {module:model/SandboxTesterV2Attributes.SubscriptionRenewalRateEnum} subscriptionRenewalRate
 */
SandboxTesterV2Attributes.prototype['subscriptionRenewalRate'] = undefined;





/**
 * Allowed values for the <code>subscriptionRenewalRate</code> property.
 * @enum {String}
 * @readonly
 */
SandboxTesterV2Attributes['SubscriptionRenewalRateEnum'] = {

    /**
     * value: "MONTHLY_RENEWAL_EVERY_ONE_HOUR"
     * @const
     */
    "MONTHLY_RENEWAL_EVERY_ONE_HOUR": "MONTHLY_RENEWAL_EVERY_ONE_HOUR",

    /**
     * value: "MONTHLY_RENEWAL_EVERY_THIRTY_MINUTES"
     * @const
     */
    "MONTHLY_RENEWAL_EVERY_THIRTY_MINUTES": "MONTHLY_RENEWAL_EVERY_THIRTY_MINUTES",

    /**
     * value: "MONTHLY_RENEWAL_EVERY_FIFTEEN_MINUTES"
     * @const
     */
    "MONTHLY_RENEWAL_EVERY_FIFTEEN_MINUTES": "MONTHLY_RENEWAL_EVERY_FIFTEEN_MINUTES",

    /**
     * value: "MONTHLY_RENEWAL_EVERY_FIVE_MINUTES"
     * @const
     */
    "MONTHLY_RENEWAL_EVERY_FIVE_MINUTES": "MONTHLY_RENEWAL_EVERY_FIVE_MINUTES",

    /**
     * value: "MONTHLY_RENEWAL_EVERY_THREE_MINUTES"
     * @const
     */
    "MONTHLY_RENEWAL_EVERY_THREE_MINUTES": "MONTHLY_RENEWAL_EVERY_THREE_MINUTES"
};



export default SandboxTesterV2Attributes;

