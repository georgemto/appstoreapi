/**
 * App Store Connect API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 4.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Nomination from './Nomination';
import NominationsResponseIncludedInner from './NominationsResponseIncludedInner';
import PagedDocumentLinks from './PagedDocumentLinks';
import PagingInformation from './PagingInformation';

/**
 * The NominationsResponse model module.
 * @module model/NominationsResponse
 * @version 4.1
 */
class NominationsResponse {
    /**
     * Constructs a new <code>NominationsResponse</code>.
     * @alias module:model/NominationsResponse
     * @param data {Array.<module:model/Nomination>} 
     * @param links {module:model/PagedDocumentLinks} 
     */
    constructor(data, links) { 
        
        NominationsResponse.initialize(this, data, links);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, data, links) { 
        obj['data'] = data;
        obj['links'] = links;
    }

    /**
     * Constructs a <code>NominationsResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NominationsResponse} obj Optional instance to populate.
     * @return {module:model/NominationsResponse} The populated <code>NominationsResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NominationsResponse();

            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], [Nomination]);
            }
            if (data.hasOwnProperty('included')) {
                obj['included'] = ApiClient.convertToType(data['included'], [NominationsResponseIncludedInner]);
            }
            if (data.hasOwnProperty('links')) {
                obj['links'] = PagedDocumentLinks.constructFromObject(data['links']);
            }
            if (data.hasOwnProperty('meta')) {
                obj['meta'] = PagingInformation.constructFromObject(data['meta']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NominationsResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NominationsResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of NominationsResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['data']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['data'])) {
                throw new Error("Expected the field `data` to be an array in the JSON data but got " + data['data']);
            }
            // validate the optional field `data` (array)
            for (const item of data['data']) {
                Nomination.validateJSON(item);
            };
        }
        if (data['included']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['included'])) {
                throw new Error("Expected the field `included` to be an array in the JSON data but got " + data['included']);
            }
            // validate the optional field `included` (array)
            for (const item of data['included']) {
                NominationsResponseIncludedInner.validateJSON(item);
            };
        }
        // validate the optional field `links`
        if (data['links']) { // data not null
          PagedDocumentLinks.validateJSON(data['links']);
        }
        // validate the optional field `meta`
        if (data['meta']) { // data not null
          PagingInformation.validateJSON(data['meta']);
        }

        return true;
    }


}

NominationsResponse.RequiredProperties = ["data", "links"];

/**
 * @member {Array.<module:model/Nomination>} data
 */
NominationsResponse.prototype['data'] = undefined;

/**
 * @member {Array.<module:model/NominationsResponseIncludedInner>} included
 */
NominationsResponse.prototype['included'] = undefined;

/**
 * @member {module:model/PagedDocumentLinks} links
 */
NominationsResponse.prototype['links'] = undefined;

/**
 * @member {module:model/PagingInformation} meta
 */
NominationsResponse.prototype['meta'] = undefined;






export default NominationsResponse;

