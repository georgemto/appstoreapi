/**
 * App Store Connect API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 4.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BuildUpload from './BuildUpload';
import BuildUploadsResponseIncludedInner from './BuildUploadsResponseIncludedInner';
import DocumentLinks from './DocumentLinks';

/**
 * The BuildUploadResponse model module.
 * @module model/BuildUploadResponse
 * @version 4.1
 */
class BuildUploadResponse {
    /**
     * Constructs a new <code>BuildUploadResponse</code>.
     * @alias module:model/BuildUploadResponse
     * @param data {module:model/BuildUpload} 
     * @param links {module:model/DocumentLinks} 
     */
    constructor(data, links) { 
        
        BuildUploadResponse.initialize(this, data, links);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, data, links) { 
        obj['data'] = data;
        obj['links'] = links;
    }

    /**
     * Constructs a <code>BuildUploadResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BuildUploadResponse} obj Optional instance to populate.
     * @return {module:model/BuildUploadResponse} The populated <code>BuildUploadResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BuildUploadResponse();

            if (data.hasOwnProperty('data')) {
                obj['data'] = BuildUpload.constructFromObject(data['data']);
            }
            if (data.hasOwnProperty('included')) {
                obj['included'] = ApiClient.convertToType(data['included'], [BuildUploadsResponseIncludedInner]);
            }
            if (data.hasOwnProperty('links')) {
                obj['links'] = DocumentLinks.constructFromObject(data['links']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BuildUploadResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BuildUploadResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BuildUploadResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `data`
        if (data['data']) { // data not null
          BuildUpload.validateJSON(data['data']);
        }
        if (data['included']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['included'])) {
                throw new Error("Expected the field `included` to be an array in the JSON data but got " + data['included']);
            }
            // validate the optional field `included` (array)
            for (const item of data['included']) {
                BuildUploadsResponseIncludedInner.validateJSON(item);
            };
        }
        // validate the optional field `links`
        if (data['links']) { // data not null
          DocumentLinks.validateJSON(data['links']);
        }

        return true;
    }


}

BuildUploadResponse.RequiredProperties = ["data", "links"];

/**
 * @member {module:model/BuildUpload} data
 */
BuildUploadResponse.prototype['data'] = undefined;

/**
 * @member {Array.<module:model/BuildUploadsResponseIncludedInner>} included
 */
BuildUploadResponse.prototype['included'] = undefined;

/**
 * @member {module:model/DocumentLinks} links
 */
BuildUploadResponse.prototype['links'] = undefined;






export default BuildUploadResponse;

