/**
 * App Store Connect API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 4.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PagedDocumentLinks from './PagedDocumentLinks';
import PagingInformation from './PagingInformation';
import ScmRepositoriesResponseIncludedInner from './ScmRepositoriesResponseIncludedInner';
import ScmRepository from './ScmRepository';

/**
 * The ScmRepositoriesResponse model module.
 * @module model/ScmRepositoriesResponse
 * @version 4.1
 */
class ScmRepositoriesResponse {
    /**
     * Constructs a new <code>ScmRepositoriesResponse</code>.
     * @alias module:model/ScmRepositoriesResponse
     * @param data {Array.<module:model/ScmRepository>} 
     * @param links {module:model/PagedDocumentLinks} 
     */
    constructor(data, links) { 
        
        ScmRepositoriesResponse.initialize(this, data, links);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, data, links) { 
        obj['data'] = data;
        obj['links'] = links;
    }

    /**
     * Constructs a <code>ScmRepositoriesResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ScmRepositoriesResponse} obj Optional instance to populate.
     * @return {module:model/ScmRepositoriesResponse} The populated <code>ScmRepositoriesResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ScmRepositoriesResponse();

            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], [ScmRepository]);
            }
            if (data.hasOwnProperty('included')) {
                obj['included'] = ApiClient.convertToType(data['included'], [ScmRepositoriesResponseIncludedInner]);
            }
            if (data.hasOwnProperty('links')) {
                obj['links'] = PagedDocumentLinks.constructFromObject(data['links']);
            }
            if (data.hasOwnProperty('meta')) {
                obj['meta'] = PagingInformation.constructFromObject(data['meta']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ScmRepositoriesResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ScmRepositoriesResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ScmRepositoriesResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['data']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['data'])) {
                throw new Error("Expected the field `data` to be an array in the JSON data but got " + data['data']);
            }
            // validate the optional field `data` (array)
            for (const item of data['data']) {
                ScmRepository.validateJSON(item);
            };
        }
        if (data['included']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['included'])) {
                throw new Error("Expected the field `included` to be an array in the JSON data but got " + data['included']);
            }
            // validate the optional field `included` (array)
            for (const item of data['included']) {
                ScmRepositoriesResponseIncludedInner.validateJSON(item);
            };
        }
        // validate the optional field `links`
        if (data['links']) { // data not null
          PagedDocumentLinks.validateJSON(data['links']);
        }
        // validate the optional field `meta`
        if (data['meta']) { // data not null
          PagingInformation.validateJSON(data['meta']);
        }

        return true;
    }


}

ScmRepositoriesResponse.RequiredProperties = ["data", "links"];

/**
 * @member {Array.<module:model/ScmRepository>} data
 */
ScmRepositoriesResponse.prototype['data'] = undefined;

/**
 * @member {Array.<module:model/ScmRepositoriesResponseIncludedInner>} included
 */
ScmRepositoriesResponse.prototype['included'] = undefined;

/**
 * @member {module:model/PagedDocumentLinks} links
 */
ScmRepositoriesResponse.prototype['links'] = undefined;

/**
 * @member {module:model/PagingInformation} meta
 */
ScmRepositoriesResponse.prototype['meta'] = undefined;






export default ScmRepositoriesResponse;

