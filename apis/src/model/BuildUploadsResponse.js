/**
 * App Store Connect API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 4.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BuildUpload from './BuildUpload';
import BuildUploadsResponseIncludedInner from './BuildUploadsResponseIncludedInner';
import PagedDocumentLinks from './PagedDocumentLinks';
import PagingInformation from './PagingInformation';

/**
 * The BuildUploadsResponse model module.
 * @module model/BuildUploadsResponse
 * @version 4.1
 */
class BuildUploadsResponse {
    /**
     * Constructs a new <code>BuildUploadsResponse</code>.
     * @alias module:model/BuildUploadsResponse
     * @param data {Array.<module:model/BuildUpload>} 
     * @param links {module:model/PagedDocumentLinks} 
     */
    constructor(data, links) { 
        
        BuildUploadsResponse.initialize(this, data, links);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, data, links) { 
        obj['data'] = data;
        obj['links'] = links;
    }

    /**
     * Constructs a <code>BuildUploadsResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BuildUploadsResponse} obj Optional instance to populate.
     * @return {module:model/BuildUploadsResponse} The populated <code>BuildUploadsResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BuildUploadsResponse();

            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], [BuildUpload]);
            }
            if (data.hasOwnProperty('included')) {
                obj['included'] = ApiClient.convertToType(data['included'], [BuildUploadsResponseIncludedInner]);
            }
            if (data.hasOwnProperty('links')) {
                obj['links'] = PagedDocumentLinks.constructFromObject(data['links']);
            }
            if (data.hasOwnProperty('meta')) {
                obj['meta'] = PagingInformation.constructFromObject(data['meta']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BuildUploadsResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BuildUploadsResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BuildUploadsResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['data']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['data'])) {
                throw new Error("Expected the field `data` to be an array in the JSON data but got " + data['data']);
            }
            // validate the optional field `data` (array)
            for (const item of data['data']) {
                BuildUpload.validateJSON(item);
            };
        }
        if (data['included']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['included'])) {
                throw new Error("Expected the field `included` to be an array in the JSON data but got " + data['included']);
            }
            // validate the optional field `included` (array)
            for (const item of data['included']) {
                BuildUploadsResponseIncludedInner.validateJSON(item);
            };
        }
        // validate the optional field `links`
        if (data['links']) { // data not null
          PagedDocumentLinks.validateJSON(data['links']);
        }
        // validate the optional field `meta`
        if (data['meta']) { // data not null
          PagingInformation.validateJSON(data['meta']);
        }

        return true;
    }


}

BuildUploadsResponse.RequiredProperties = ["data", "links"];

/**
 * @member {Array.<module:model/BuildUpload>} data
 */
BuildUploadsResponse.prototype['data'] = undefined;

/**
 * @member {Array.<module:model/BuildUploadsResponseIncludedInner>} included
 */
BuildUploadsResponse.prototype['included'] = undefined;

/**
 * @member {module:model/PagedDocumentLinks} links
 */
BuildUploadsResponse.prototype['links'] = undefined;

/**
 * @member {module:model/PagingInformation} meta
 */
BuildUploadsResponse.prototype['meta'] = undefined;






export default BuildUploadsResponse;

